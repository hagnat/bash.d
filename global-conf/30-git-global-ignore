#!/bin/bash

touch ~/.gitignore_global

git config --global core.excludesfile ~/.gitignore_global

grep "# autogenerated by bash.d" ~/.gitignore_global >/dev/null

if [[ 1 == $? ]]; then
    cat ~/.bash.d/templates/gitignore_global >> ~/.gitignore_global
fi
